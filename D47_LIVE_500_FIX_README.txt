DEVA D47 LIVE PRODUCTS 500 FIX
1. Ensures and verifies products.product_code on persistent Render SQLite.
2. Preserves existing database product data.
3. Loads the 155 built-in products from server/src/builtin-products.json.
4. Falls back automatically to data.js if the JSON file is missing on Render.
5. Adds [D47] log markers so the deployed version is easy to verify.

DEVA Admin session UI fix
- Added a strong [hidden] CSS rule so Login and Dashboard do not display at the same time.
- Clear stale login error after a valid session is confirmed.
- Backend authentication/session logic unchanged.

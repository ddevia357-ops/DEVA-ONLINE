(()=>{const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));function dest(x){if(x.cta_url)return x.cta_url;if(x.whatsapp){const n=String(x.whatsapp).replace(/\D/g,'');return 'https://wa.me/'+n}return '#'}async function init(){try{const a=await fetch('/api/sponsors/active?placement=HOME').then(r=>r.ok?r.json():[]);if(!a.length)return;const sec=$('#sponsoredHome'),grid=$('#sponsoredHomeGrid');sec.hidden=false;grid.innerHTML=a.map(x=>`<article class="sponsor-card" data-sponsor-view="${x.id}">${x.image?`<img src="${esc(x.image)}" alt="${esc(x.company_name)}">`:''}<div class="sponsor-card-body"><small>SPONSORED</small><b>${esc(x.company_name)}</b><h3>${esc(x.title)}</h3><p>${esc(x.description||'')}</p><a data-sponsor-click="${x.id}" href="${esc(dest(x))}" target="_blank" rel="noopener sponsored">${esc(x.cta_label||'زیاتر ببینە')} ↗</a></div></article>`).join('');a.forEach(x=>fetch('/api/sponsors/'+x.id+'/view',{method:'POST',keepalive:true}).catch(()=>{}));grid.querySelectorAll('[data-sponsor-click]').forEach(a=>a.addEventListener('click',()=>fetch('/api/sponsors/'+a.dataset.sponsorClick+'/click',{method:'POST',keepalive:true}).catch(()=>{})))}catch{}}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init()})();